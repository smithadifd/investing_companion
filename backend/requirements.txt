# ===========================================
# Core Framework
# ===========================================
fastapi>=0.136.1
uvicorn[standard]>=0.51.0
python-multipart>=0.0.6
python-dotenv>=1.2.2

# ===========================================
# Database
# ===========================================
sqlalchemy[asyncio]>=2.0.49
asyncpg>=0.29.0
alembic>=1.18.4
psycopg2-binary>=2.9.12  # For alembic sync operations

# ===========================================
# Validation & Serialization
# ===========================================
pydantic>=2.5.0
pydantic-settings>=2.15.0
email-validator>=2.1.0

# ===========================================
# Authentication & Security
# ===========================================
argon2-cffi>=25.1.0
PyJWT>=2.8.0
cryptography>=49.0.0

# ===========================================
# Task Queue
# ===========================================
celery[redis]>=5.6.3
redis>=5.0.0

# ===========================================
# Data & Finance
# ===========================================
# Pinned exactly: the floating `>=0.2.36` floor let the resolver drift across a
# yfinance change in `dividendYield` scale (fraction in older releases, percent
# in the 1.x line) that renders/stores the yield 100x wrong. 1.1.0 is the
# version this repo currently resolves to and is verified against; the
# provider-boundary normalization in data_providers/yahoo.py keeps the canonical
# (fraction) scale correct regardless. Bumping is a separate, tested change.
yfinance==1.1.0
pandas>=2.2.0
numpy>=1.26.0
ta>=0.11.0  # Technical analysis library
schwab-py>=1.5.1  # Opt-in brokerage ingestion (OAuth); quotes only if opted in

# ===========================================
# HTTP Client
# ===========================================
httpx>=0.28.1
aiohttp>=3.9.0

# ===========================================
# AI Integration
# ===========================================
anthropic>=0.116.0

# ===========================================
# Utilities
# ===========================================
tenacity>=9.1.4  # Retry logic
pytz>=2024.1

# ===========================================
# Development & Testing
# ===========================================
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.28.1  # For TestClient
ruff==0.16.0  # Linting; pinned + paired with backend/ruff.toml so local == CI
mypy>=1.8.0  # Type checking
